# Installed fresh in the image (npm ci)
node_modules

# Built inside the image (vite build)
dist

# Local data must never ship in an image
data

# Not needed to build or run
.git
.gitignore
.claude
.env
.env.*
*.log
.DS_Store
docs
tests
vitest.config.ts
Dockerfile
.dockerignore
fly.toml
